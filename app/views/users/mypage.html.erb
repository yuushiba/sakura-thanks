<div class="min-h-screen bg-base-100">
  <div class="container mx-auto px-4 py-8">
    <h1 class="text-2xl font-bold text-center mb-8" style="color: #073472;">マイページ</h1>
    
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      <% if @posts.present? %>
        <%= render partial: 'posts/post', collection: @posts %>
      <% else %>
        <p class="text-center col-span-3">投稿がありません</p>
      <% end %>
    </div>

    <div class="text-center mt-8 flex justify-center gap-4">
      <%= link_to "新規投稿をする", new_post_path, 
          class: "btn custom-text border-none hover:opacity-80", 
          style: "background-color: #DD7594;" %>
          
      <a href="https://twitter.com/intent/tweet?url=<%= CGI.escape(root_url) %>&text=<%= CGI.escape('Sakura Thanksで感謝の気持ちを伝えています！') %>" 
        target="_blank" 
        rel="noopener" 
        class="btn custom-text border-none hover:opacity-80"
        style="background-color: #4981CF;">
        Xでシェアする
      </a>
    </div>
  </div>
</div>
